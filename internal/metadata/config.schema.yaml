# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for pkiengine metrics.
    type: object
    properties:
      pkiengine.cert.x509.not_after:
        description: "PkiengineCertX509NotAfterMetricConfig provides config for the pkiengine.cert.x509.not_after metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.cert.x509.not_before:
        description: "PkiengineCertX509NotBeforeMetricConfig provides config for the pkiengine.cert.x509.not_before metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.cache.evictions:
        description: "PkiengineCrlCacheEvictionsMetricConfig provides config for the pkiengine.crl.cache.evictions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.cache.hits:
        description: "PkiengineCrlCacheHitsMetricConfig provides config for the pkiengine.crl.cache.hits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.cache.misses:
        description: "PkiengineCrlCacheMissesMetricConfig provides config for the pkiengine.crl.cache.misses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.processing_status:
        description: "PkiengineCrlProcessingStatusMetricConfig provides config for the pkiengine.crl.processing_status metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.x509.next_update:
        description: "PkiengineCrlX509NextUpdateMetricConfig provides config for the pkiengine.crl.x509.next_update metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.x509.revoked_certificates:
        description: "PkiengineCrlX509RevokedCertificatesMetricConfig provides config for the pkiengine.crl.x509.revoked_certificates metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.crl.x509.this_update:
        description: "PkiengineCrlX509ThisUpdateMetricConfig provides config for the pkiengine.crl.x509.this_update metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.issuer.errors:
        description: "PkiengineIssuerErrorsMetricConfig provides config for the pkiengine.issuer.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.issuer.x509.not_after:
        description: "PkiengineIssuerX509NotAfterMetricConfig provides config for the pkiengine.issuer.x509.not_after metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.issuer.x509.not_before:
        description: "PkiengineIssuerX509NotBeforeMetricConfig provides config for the pkiengine.issuer.x509.not_before metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.mount.certificates_stored:
        description: "PkiengineMountCertificatesStoredMetricConfig provides config for the pkiengine.mount.certificates_stored metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pkiengine.mount.errors:
        description: "PkiengineMountErrorsMetricConfig provides config for the pkiengine.mount.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for pkiengine resource attributes.
    type: object
    properties:
      engine.address:
        description: ResourceAttributeConfig provides common config for a engine.address resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      engine.namespace:
        description: ResourceAttributeConfig provides common config for a engine.namespace resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for pkiengine metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
